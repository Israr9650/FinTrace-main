# GraphLens Frontend

React + Vite frontend for the GraphLens application.

## Local Commands

* `npm run dev` - Starts the Vite development server on `http://localhost:5173`
* `npm run build` - Builds production bundle to `dist/`
